# Changelog

## [1.3.0] - 2026-09-22

### Added

- 在既有任务添加区新增“一次性／每天／每周／每月／每年”属性；一次性仍为默认，重复主任务必须设置首次提醒时间。
- 新增本地 04:00 周期边界：每天 04:00、每周一 04:00、每月 1 日 04:00、每年 1 月 1 日 04:00 自动刷新。
- 后续提醒按首次提醒锚点投影，支持月末夹取与闰日恢复；启动、窗口恢复可见及运行跨日时执行幂等补刷新。
- 未完成周期会生成一次性承接子任务；离线错过多期时合并为一个带期数范围的子任务。
- 主界面和桌面任务笺新增重复规则、上期未完成标签；任务笺快照只包含格式化文本，不暴露可写周期元数据。
- 提醒指纹本地持久化，当前周期到期提醒在程序重启后不会重复发送。

### Fixed

- 修复亮度值 `100%` 因容器宽度不足换行错位的问题。
- 捕获原生通知桥的异步失败，保留应用内提醒且不产生未处理的 Promise 拒绝。

### Compatibility

- 原有任务自动按一次性任务读取，`smart_tasks`、运行数据目录和附件目录保持不变。
- SmartTodo 完全退出后不会在后台发送通知；重新启动时会为当前未提醒的到期周期补发一次。

### Testing

- `npm test`：147 项，145 项通过，2 项因当前 Windows 环境不支持创建符号链接而明确跳过，0 项失败。
- `npm run test:ui`：周期刷新／重载幂等、提醒去重、8 组主界面尺寸／缩放及任务笺 100%／125%／150% 缩放全部通过。

## [1.2.2] - 2026-09-22

### Fixed

- 修复 Windows 上双击提醒时间“分钟”分段时可能触发整个 Electron 窗口黑屏的问题：生产程序改用稳定的软件合成，并在第二次按下时阻止原生分段文本的异常重绘路径。
- 修复亮度按钮与悬浮拉条之间存在空隙、鼠标移动到拉条前浮层就消失的问题；增加连续悬停桥与 180ms 离开容错。

### Improved

- 为按钮、筛选、任务复选框和输入控件补充克制的按压、聚焦与选中反馈，并继续尊重系统“减少动态效果”设置。
- 任务筛选增加同步的 `aria-pressed` 状态，鼠标、键盘和辅助技术获得一致反馈。

### Testing

- `npm test`：122 项，120 项通过，2 项因当前 Windows 环境不支持创建符号链接而明确跳过，0 项失败。
- `npm run test:ui`：提醒时间双击保护、亮度浮层悬停桥、筛选状态、8 组响应式尺寸／缩放及桌面任务笺测试全部通过。

## [1.2.1] - 2026-09-21

### Fixed

- 修复任务行“子任务”与折叠、优先级等操作控件在部分尺寸下交叉错位的问题。
- 修复主界面任务笺按钮只能显示、无法再次点击收起的问题，并在启动时同步真实可见状态。
- 修复知行助手连续消息和头像因负边距、强制位移导致的交叉现象。
- 修复顶部工具区基线错位；桌面任务笺移除重复入口，并改为可透出桌面的克制毛玻璃表面。
- 发布文件改用独立的 `1.2.1` 文件名与 `release/V1.2.1` 目录，避免与旧 `1.2.0` 产物混淆。

### Testing

- `npm test`：119 项，117 项通过，2 项因当前 Windows 不支持符号链接而跳过，0 项失败。
- `npm run test:ui`：主界面行为、8 组响应式尺寸／缩放和真实桌面任务笺双窗口测试全部通过。
- 验证任务操作区与助手消息无交叉、顶部控件中心线一致，以及任务笺主界面按钮可双向开关。

## [1.2.0] - 2026-09-21

### Added

- 新增 320×440 桌面任务笺，最多展示 5 项未完成任务，并支持完成、打开主界面与收起。
- 新增任务笺托盘开关、主界面入口、位置／可见性记忆和断开显示器后的安全回收。
- 新增任务笺脱敏快照、固定 IPC、发送者校验、请求超时与保存失败回滚。
- 新增真实双窗口、跨分辨率与高 DPI Electron 验收矩阵。

### Changed

- 品牌名称更新为 SmartTodo，重新绘制竹叶流萤应用、托盘与界面图标。
- 主界面更新为“文房案台／漆夜流萤”现代中式深浅双主题。
- 重构任务工作台、知行助手、筛选、附件、对话框、状态反馈和长内容布局。
- 增加本地 GSAP 状态动效与完整 `prefers-reduced-motion` 回退。
- 最小窗口调整为 800×620，并优化 1366×768 至 2560×1440、窄窗口和 125%／150% 缩放。

### Compatibility

- 保持 V1.0.7 的 `localStorage` 键名、任务字段、会话、草稿和 AI 配置兼容。
- 保持 `%APPDATA%\智能任务管家\运行数据` 与受管附件目录不变，无需迁移。
- 继续使用 `contextIsolation: true`、`nodeIntegration: false` 与固定附件安全边界。

### Testing

- `npm test`：115 项，113 项通过，2 项因当前 Windows 不支持符号链接而跳过，0 项失败。
- `npm run test:ui`：主界面行为、8 组响应式尺寸／缩放和真实桌面任务笺双窗口测试全部通过。
- 桌面任务笺在 100%／125%／150% 缩放下保持零横向溢出、不置顶、不抢焦点，并验证成功同步与持久化失败回滚。
