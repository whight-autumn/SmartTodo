# 文件说明 · 智能任务管家

> 本文档说明仓库内**每一份代码文件**的职责，帮助新使用者快速定位功能模块。
> 项目为 Electron 桌面应用，采用经典三层结构：主进程（Node.js）、preload 桥接、渲染进程（HTML/CSS/原生 JS）。
> 当前文档版本：V1.0.5（2026-09-14）。功能、文件名或发布结构发生变化时应同步更新本文档。

---

## 一、根目录

| 文件 | 职责 |
| --- | --- |
| `main.js` | **Electron 主进程入口**。创建主窗口、系统托盘与右键菜单；处理「关闭窗口不退出、驻留托盘」逻辑；注册原生通知、版本号、运行数据路径等 IPC 接口；启动时迁移旧版本运行数据；单实例锁防止重复启动。 |
| `main-paths.js` | 集中定义并导出一个纯函数 `buildManagedUserDataPath`，用于拼接统一的运行数据目录 `%APPDATA%\智能任务管家\运行数据`，便于主进程引用与单元测试。 |
| `preload.js` | **preload 桥接脚本**。通过 `contextBridge` 以受限方式向渲染进程暴露 `window.desktop` 对象（原生通知、版本查询、数据路径、窗口显示事件、平台信息），保持 `contextIsolation: true`、关闭 `nodeIntegration` 的安全边界。 |
| `package.json` | 项目元数据与脚本。声明 Electron / electron-builder 依赖；定义 `start`（运行）、`test`（单元测试）、`test:ui`（界面冒烟测试）、`dist`（打包 Windows 便携版）等命令；内含 electron-builder 打包配置。 |
| `package-lock.json` | 锁定依赖版本，确保 `npm install` 安装结果一致。勿手动编辑。 |
| `README.md` | 项目介绍、功能特性、快速开始、项目结构与发布说明。 |
| `.gitignore` | Git 忽略规则：排除 `node_modules/`、构建产物、`*.exe` 等不应入库的内容。 |

## 二、renderer/ 渲染进程（界面与业务逻辑）

| 文件 | 职责 |
| --- | --- |
| `index.html` | 应用唯一页面。包含任务待办面板、AI 助手面板、设置弹窗、删除确认弹窗等全部界面骨架；按顺序引入 5 个渲染脚本。 |
| `style.css` | 全部界面样式：深浅两套主题（`data-theme`）、竹青/墨色/绛红/靛蓝/淡金配色、亮度混合变量（`--ui-brightness-*`）、任务卡片、聊天框、弹窗与响应式布局。 |
| `app.js` | **渲染进程主逻辑**（约 1460 行）。负责：任务增删改查、子任务挂载、优先级排序、置顶、折叠、完成/撤销、需关注/进行中/已完成筛选与实时计数、15 天自动清理、提醒轮询与系统通知、AI 会话管理（新建/清理/流式对话/附件上传与文本预览）、主题与亮度控制、草稿恢复、版本号显示。通过 `window.TaskModel` 等模块完成纯逻辑。 |
| `task-model.js` | **任务纯逻辑模块**（可被 `node:test` 直接测试）。提供任务规范化、排序、父子索引、完成/撤销、过期清理、深度计算，以及“需关注”判定、父链可见集合和筛选数量统计。同时挂载到 `window.TaskModel` 与 `module.exports`。 |
| `draft-store.js` | **任务表单草稿模块**。在输入变化/窗口隐藏时保存未提交的表单内容（标题、备注、提醒、优先级、父任务），页面重新打开后可恢复。兼容 `localStorage` 与 Map 等存储实现。 |
| `ai-provider.js` | **AI 协议适配模块**。封装 OpenAI-compatible Chat Completions：DeepSeek/OpenAI/自定义三套预设（`PROVIDER_PRESETS`）、配置规范化、`/chat/completions` 地址拼接、SSE 流式增量解析（`parseSseChunk`）。挂载为 `window.AIProvider` 并支持 `module.exports`。 |
| `ui-appearance.js` | **界面外观纯逻辑模块**。管理 75%–125% 亮度滑杆的归一化、存储与读写（`normalizeBrightness` / `resolveBrightness` / `loadBrightness` / `saveBrightness`），以及任务时间戳格式化（`formatTaskTimestamp`）。 |

## 三、tests/ 自动化测试

| 文件 | 职责 | 运行方式 |
| --- | --- | --- |
| `task-model.test.js` | 覆盖任务完成时间记录/撤销、15 天清理、父子索引排序。 | `npm test` |
| `task-filter.test.js` | 覆盖“需关注”的 24 小时提醒规则、高优先级/置顶判定、父任务上下文与页签计数。 | `npm test` |
| `ai-provider.test.js` | 覆盖服务商预设、URL 拼接、SSE 增量解析。 | `npm test` |
| `draft-store.test.js` | 覆盖草稿写入、读取、清除的往返行为。 | `npm test` |
| `runtime-path.test.js` | 覆盖运行数据目录拼接结果。 | `npm test` |
| `ui-appearance.test.js` | 覆盖亮度范围约束、亮度混合结果、存储往返与时间戳格式化。 | `npm test` |
| `ui-smoke.cjs` | **界面冒烟测试**：在隔离临时数据目录中启动 Electron 真实窗口，检查任务筛选及计数、父子上下文、主题切换、创建/完成时间、亮度调节与标签对比度，并输出两张截图。文件名不绑定版本号，供后续迭代持续维护。 | `npm run test:ui` |

## 四、assets/ 静态资源

| 文件 | 职责 |
| --- | --- |
| `icon.png` | 应用主图标（窗口、托盘、通知、打包共用，PNG 格式）。 |
| `icon.ico` | Windows 安装/打包图标（electron-builder `win.icon` 引用，ICO 格式）。 |

## 五、release/ 发布目录

| 文件 | 职责 |
| --- | --- |
| `V1.0.5/智能任务管家-1.0.5.exe` | Windows x64 便携版交付文件，双击即用，无需安装。该文件已通过 `.gitignore` 排除，建议通过 **GitHub Releases** 发布。 |
| `V1.0.5/版本说明.txt` | 当前版本更新日志、功能说明、数据位置与升级提示。 |

## 六、docs/ 文档

| 文件 | 职责 |
| --- | --- |
| `文件说明.md` | 本文档，逐份说明代码文件的职责。 |

---

## 模块间调用关系

```
main.js ──require──▶ main-paths.js        （主进程：窗口/托盘/通知/生命周期）
main.js ──preload──▶ preload.js           （contextBridge 安全桥接）
                          │
renderer/index.html ◀── window.desktop    （渲染进程通过 IPC 调用原生能力）
renderer/index.html ──引入──▶ app.js
app.js ──window.TaskModel──▶ task-model.js     （任务纯逻辑）
app.js ──window.DraftStore──▶ draft-store.js   （表单草稿）
app.js ──window.AIProvider──▶ ai-provider.js   （AI 协议适配）
app.js ──window.UIAppearance──▶ ui-appearance.js（外观/亮度）
tests/*.test.js ──require──▶ renderer/*.js      （纯逻辑模块可直接单测）
```

> 说明：`task-model.js`、`draft-store.js`、`ai-provider.js`、`ui-appearance.js` 采用「UMD 风格」——浏览器环境下挂载到 `window`，Node 环境下通过 `module.exports` 导出，因此既能被页面直接使用，也能被 `node:test` 单元测试加载。
