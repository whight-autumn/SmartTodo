# 智能任务管家 V1.0.6 任务备注与附件设计

日期：2026-09-14
状态：已批准
目标版本：V1.0.6

## 1. 目标

为已经创建的主任务和子任务提供可重复编辑的备注，并允许备注携带图片或普通文件。编辑行为不得改变任务创建时间或完成时间；当备注文本或附件发生实际变化时，单独记录最近编辑时间。备注中的 HTTP/HTTPS 链接应可通过系统默认浏览器安全打开。

每个任务最多保存 10 个附件，单个附件不超过 20MB，与 AI 助手当前限制保持一致。附件必须由程序复制到统一运行数据目录，不能依赖原文件继续存在，也不能写入 localStorage。

## 2. 不在本次范围内

- 不提供在线文件同步、云端备份或多人共享。
- 不修改任务标题；优先级仍使用任务卡片上的现有下拉框修改。
- 不实现富文本编辑器、Markdown 编辑器或网页内容抓取。
- 不自动访问备注中的网址，只在用户明确点击后打开。
- 不改变现有创建时间、完成时间、提醒时间和 15 天归档清理规则。

## 3. 用户界面

每张主任务和子任务卡片增加“编辑备注”按钮。点击后打开统一编辑弹窗，弹窗包含当前备注文本、已有附件列表、添加附件按钮、移除附件操作、取消和保存按钮。

已保存图片在弹窗和任务卡片备注区域显示惰性缩略图，待保存图片在弹窗内通过临时 object URL 显示真实缩略图并在移除、取消或成功保存后释放；普通文件显示文件名、MIME/文件类型和易读大小。卡片与弹窗都会检查附件可用性，缺失或不安全时明确显示“文件已不存在”。附件操作失败时不关闭编辑弹窗，恢复可用控件，也不丢失用户正在编辑的文字。

任务卡片继续显示“创建”和“完成”时间；发生有效编辑后新增“编辑 YYYY-MM-DD HH:mm”。打开弹窗但未修改、修改后还原原值或直接取消时，不更新时间戳。

备注按纯文本保存和渲染。渲染时识别 `http://` 与 `https://` 地址并生成链接，其余协议保持普通文本。点击链接后通过主进程调用系统默认浏览器，页面自身不直接导航。

## 4. 任务数据结构

现有任务增加两个可选字段：

```js
{
  updatedAt: null,
  attachments: [
    {
      id: "随机附件标识",
      name: "原始文件名.png",
      storageName: "随机存储名.png",
      mimeType: "image/png",
      size: 102400,
      addedAt: 1789372800000
    }
  ]
}
```

任务只保存附件元数据和随机存储名，不保存原始绝对路径。`normalizeTasks` 为 V1.0.5 及更早任务补充 `updatedAt: null` 与 `attachments: []`，并过滤格式错误的附件记录。旧数据首次加载后保持兼容，不重写已有创建时间和完成时间。

只有备注文本或附件集合发生实际变化时才写入新的 `updatedAt`。优先级、置顶、折叠、完成或撤销完成不属于备注编辑，不改变 `updatedAt`。

## 5. 附件存储与生命周期

附件根目录：

```text
%APPDATA%\智能任务管家\运行数据\task-attachments\<任务ID>\
```

文件使用随机标识作为磁盘文件名，并保留安全扩展名。原始文件名仅作为界面元数据。所有磁盘路径都由主进程根据任务 ID 和存储名构造，并通过解析后的绝对路径检查确保结果仍位于附件根目录内。

新增根目录模块 `task-attachment-store.js`，负责限制校验、安全文件名、复制、受管路径解析、读取图片预览地址、事务删除和清理整个任务目录。首次使用时固定附件根目录的规范路径和文件身份，此后每次变更或解析均拒绝符号链接、联接、非目录或身份替换。每个任务的导入、删除及整任务清理共用串行队列；每次从任务目录实际条目重新计数，`existingCount` 仅为兼容字段而不是安全依据。

打开或生成本地预览 URL 前，存储模块必须对任务目录和目标条目执行 `lstat`，拒绝符号链接及非目录/非普通文件，再通过 `realpath` 确认实际目标仍位于附件根目录。缺失文件也必须拒绝，由界面转换为明确的不可用状态。

删除单个附件时同步删除磁盘文件。删除任务及其子任务、或者 15 天清理机制移除已完成任务时，调用主进程清理对应任务目录。缺失的附件文件不阻止任务加载，界面显示“文件已不存在”，并允许用户移除该失效记录。

## 6. Electron 安全边界

`main.js` 初始化附件存储，并通过独立的 `task-attachment-ipc.js` 注册固定的准备、提交、回滚、清理、解析、打开和外链能力。`preload.js` 不暴露路径转换或接收 `sourcePaths` 的 API，只接收文件选择框产生的 `File`，在隔离上下文中调用 `webUtils.getPathForFile`；伪造对象、非 `File` 和空路径在 IPC 前拒绝。

- 导入用户在文件选择框中明确选择的文件。
- 打开程序附件目录内的指定文件。
- 获取程序附件目录内图片的本地预览地址。
- 删除指定附件或指定任务的附件目录。
- 使用系统默认浏览器打开经过校验的 HTTP/HTTPS 地址。

主进程再次执行单文件 20MB、每任务 10 个文件的限制，不能只依赖前端校验。任务 ID、附件 ID、存储名和协议都必须验证；拒绝路径穿越、符号链接逃逸、`file:`、`javascript:` 等非 HTTP/HTTPS 地址。错误信息不暴露运行数据目录外的敏感路径。

## 7. 编辑保存流程

1. 用户打开编辑弹窗，界面复制当前备注与附件元数据作为草稿。
2. 新选择文件仅进入待导入列表；移除已有附件仅进入待删除列表。
3. 用户取消时丢弃草稿，不执行文件操作，也不更新时间戳。
4. 用户保存时，preload 从实际选择的 `File` 派生路径；主进程复制新增文件并把可删除旧文件移动到私有事务暂存区，返回成功/失败移除项。
5. 渲染进程根据准备结果计算完整的下一份任务快照，并先写入 `localStorage`，不提前替换内存任务。
6. 持久化成功后提交事务，永久清理暂存删除；部分移除失败的元数据继续保留，并从已保存任务重建开放弹窗。
7. 持久化失败时回滚事务，删除本次导入并把暂存旧文件恢复原位；内存任务和持久化任务均保持原样，弹窗继续保留草稿并恢复控件。
8. 备注文本、成功导入或成功删除任一发生时才更新 `updatedAt`；`createdAt` 与 `completedAt` 始终不变。

## 8. 性能与界面风格

附件内容不写入 localStorage，不参与任务列表签名序列化；任务签名只使用附件元数据，避免大文件拖慢渲染。图片缩略图仅在可见任务或打开的编辑弹窗中加载，限制显示尺寸并使用惰性加载。

视觉延续竹青、墨色、淡金和纸白配色。编辑按钮、附件卡片和链接保持简洁，不引入新的高饱和主色；深浅主题均需有清晰边界、悬停状态和键盘焦点。

## 9. 测试标准

- 任务模型测试：旧任务迁移、附件元数据规范化、有效编辑更新时间、创建/完成时间保持不变。
- preload 测试：不暴露任意路径 API，实际 `File` 派生路径，伪造/非 `File`/空路径在 IPC 前拒绝。
- 附件存储测试：20MB 边界、忽略伪低 `existingCount` 的重复调用 10 个限制、事务提交/回滚、根目录身份与替换、缺失/非普通/符号链接受管条目、整任务清理串行化。
- 附件 IPC 测试：固定事务通道、严格 payload/事务 ID 校验、安全存储委托、系统打开错误和 HTTP/HTTPS 协议限制。
- 链接测试：只识别 HTTP/HTTPS，拒绝其他协议并正确处理中文标点后的网址。
- Electron 界面冒烟测试：打开编辑弹窗、保存备注、再次编辑、取消不更新时间、链接渲染、已有/待保存图片与 MIME/类型、缺失状态，以及导入、部分删除和持久化失败后的开放弹窗、控件、磁盘与元数据一致性。
- 完整执行 `npm test`、`npm run test:ui` 和 `npm run dist`，确认 Windows x64 便携版版本信息为 1.0.6。

## 10. 文档、发布与版本控制

同步更新 `README.md`、`docs/文件说明.md` 和 `release/V1.0.6/版本说明.txt`，说明附件限制、存储位置、隐私边界、编辑时间和链接行为。当前源码树只保留 V1.0.6 发布说明；历史版本由 Git 历史与 GitHub Releases 保存。

最终源码提交信息使用 `feat: V1.0.6微调`。实现任务在隔离工作树中准备源码提交和 `release/V1.0.6` 交付内容；控制器随后负责更新 `main`、创建 GitHub Release `v1.0.6` 并上传 `智能任务管家-1.0.6.exe` 与版本说明。exe 继续由 `.gitignore` 排除，不进入源码提交。
