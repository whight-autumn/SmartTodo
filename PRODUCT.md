# Product

<!-- impeccable:product-schema 1 -->

## Platform

Windows desktop (Electron)

## Users

主要用户是在 Windows 桌面环境中独立管理工作与生活事务的个人用户。他们希望快速记录、安排、追踪任务，并在需要时让 AI 协助梳理优先级和时间安排，同时不把个人任务数据交给第三方产品账户。

## Product Purpose

SmartTodo 是一款本地优先的桌面任务与 AI 助手。它让用户在一个常驻、低打扰的工作台中管理主任务、子任务、提醒、备注和附件，并按需调用用户自行配置的 OpenAI-compatible 服务分析任务或进行对话。成功意味着常用任务操作足够快、长期数据可靠、升级不破坏既有内容，且离线时仍能完整管理任务。

## Positioning

产品将完整任务管理能力、受管本地附件和可选 AI 助手组合在一个无需注册、无需云端账户的 Windows 桌面应用中。AI 是用户主动配置的增强能力，不是任务管理和数据可用性的前提。

## Operating Context

- 应用在 Windows 桌面上长期运行，并可驻留系统托盘。
- 用户会在不同窗口尺寸、Windows 显示缩放比例以及深浅主题之间切换。
- 任务、设置、会话和 API Key 保存在本机运行数据目录中。
- 任务附件复制到应用管理的本地目录，不依赖原文件继续存在。
- 用户可能长时间积累任务、备注、附件和 AI 会话，因此升级兼容与清晰状态反馈比短期视觉新奇更重要。

## Capabilities and Constraints

- 保留主任务与子任务、提醒、高中低优先级、置顶、折叠、完成与撤销。
- 保留“需关注／进行中／已完成”筛选、15 天完成任务自动清理和层级内智能排序。
- 保留可重复编辑的备注、安全 HTTP/HTTPS 链接、受管附件、图片缩略图和缺失文件状态。
- 保留 DeepSeek、OpenAI 和自定义 OpenAI-compatible 服务、流式输出、会话管理、任务分析与对话附件。
- 保留深浅主题、75%–125% 亮度、系统通知、托盘常驻和单实例运行。
- 桌面任务笺最多展示 5 项未完成任务，按已逾期、24 小时内到期、置顶、高优先级和其余任务排序；只允许完成、打开主界面与收起。
- 桌面任务笺默认位于工作区右侧，记忆位置并在显示器变化后回到可见范围；不置顶、不抢占焦点，主界面仍是任务数据的唯一写入者。
- 不新增账号、云同步、多人协作、远程遥测或后端服务。
- V1.3.1 必须保留 V1.0.7 至 V1.3.0 的 `localStorage` 键名、任务数据和附件结构，并把历史运行目录安全迁移到 SmartTodo 目录。
- 渲染进程继续使用 `contextIsolation: true` 与 `nodeIntegration: false`；附件事务、安全外链和文件边界不得弱化。
- 应用需要在 1366×768 到 2560×1440 的常见桌面屏幕、Windows 125%／150% 缩放以及较窄窗口中保持完整可用，不得因拉伸发生重叠、裁切、横向撕裂或不可达操作。

## Brand Commitments

- 产品名称及应用内外品牌统一为“SmartTodo”，功能定位为本地优先任务与 AI 助手，版本归属为“萤火”。
- 品牌识别保留“竹叶与流萤”这一核心记忆，但允许为 V1.2 重新绘制并统一应用内外图形。
- 视觉气质固定为细腻、低调、优雅、现代新中式；企业级品质来自比例、材质、排版和交互完整度，不使用夸张装饰营造豪华感。

## Evidence on Hand

- 当前产品说明与功能事实：`README.md`。
- 文件职责与安全边界：`docs/文件说明.md`。
- 当前应用图标：`assets/icon.png` 与 `assets/icon.ico`。
- 当前唯一界面与业务行为：`renderer/index.html`、`renderer/style.css`、`renderer/app.js`。
- 自动化证据：`tests/*.test.js`、`tests/ui-smoke.cjs`、`tests/ui-layout.cjs` 与 `tests/ui-widget.cjs`；V1.2 为 115 项单元测试，其中 113 项通过、2 项因运行环境不支持符号链接而跳过、0 项失败，并包含真实 Electron 双窗口与响应式矩阵验证。
- 未提供客户评价、商业数据、团队协作需求或云服务证明；后续工作不得虚构这些内容。

## Product Principles

1. 本地任务管理始终独立于 AI 和网络可用性。
2. 高频操作应清晰、快速、可撤销，界面装饰不得妨碍任务流。
3. 数据兼容、安全边界和附件一致性优先于视觉重构便利。
4. 状态变化必须可感知且不过度打扰，复杂能力通过渐进呈现保持易用。
5. 不同窗口尺寸、主题、缩放比例和输入方式下都提供一致完整的操作能力。

## Accessibility & Inclusion

- 保持完整键盘导航、清晰焦点状态、语义化标签与屏幕阅读器可识别名称。
- 正文和关键控件满足 WCAG AA 对比度目标；不能只依赖颜色表达优先级、错误或完成状态。
- 尊重 `prefers-reduced-motion`，在减少动态效果时取消非必要位移和过渡。
- 长标题、长备注、长 URL、长文件名和中文输入法组合输入不得破坏布局或提交行为。
